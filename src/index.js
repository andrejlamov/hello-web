import {Map} from 'immutable';
import React from 'react';
import ReactDOM from 'react-dom';
import './style.css';
import {HelloButton} from './Button.jsx';

/* const data = Map({msg: "Hello dev server!"});
 * ReactDOM.render(
 *   <HelloButton />,
 *   document.getElementById('root')
 * );
 *  */
export default {
  HelloButton
};
